{{ form_start(form) }}
<div class="row form-group">
    <div class="col-12 mt-3">{{ form_row(form.nom) }}</div>
</div>
<div class="mt-5 text-end">
    {{ form_widget(form) }}
    <a href="{{ path('app_brasserie_index') }}" class="btn btn-outline-secondary">Annuler</a>
    <button class="btn btn-warning">{{ button_label|default('Enregistrer') }}</button>
</div>

{{ form_end(form) }}
