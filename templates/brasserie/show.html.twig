{% extends 'base.html.twig' %}

{% block title %}Brasserie{% endblock %}

{% block body %}
    <h1>Brasserie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ brasserie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ brasserie.nom }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ brasserie.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_brasserie_index') }}">back to list</a>

    <a href="{{ path('app_brasserie_edit', {'id': brasserie.id}) }}">edit</a>

    {{ include('brasserie/_delete_form.html.twig') }}
{% endblock %}
