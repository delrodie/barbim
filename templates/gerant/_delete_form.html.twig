<form method="post" action="{{ path('app_gerant_delete', {'id': gerant.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce gérant?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gerant.id) }}">
    <button class="btn btn-danger mt-5">Supprimer</button>
</form>
