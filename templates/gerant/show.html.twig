{% extends 'base.html.twig' %}

{% block title %}Gerant{% endblock %}

{% block body %}
    <h1>Gerant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gerant.id }}</td>
            </tr>
            <tr>
                <th>Matricule</th>
                <td>{{ gerant.matricule }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ gerant.nom }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ gerant.telephone }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ gerant.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_gerant_index') }}">back to list</a>

    <a href="{{ path('app_gerant_edit', {'id': gerant.id}) }}">edit</a>

    {{ include('gerant/_delete_form.html.twig') }}
{% endblock %}
