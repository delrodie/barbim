{{ form_start(form) }}
    <div class="row form-group">
        <div class="col-12 mt-3">{{ form_row(form.gerant) }}</div>
        <div class="col-12 mt-3">{{ form_row(form.dateRecette) }}</div>
    </div>
    <div class="text-end mt-5">
        {{ form_widget(form) }}
        <a href="{{ path('app_recette_index') }}" class="btn btn-outline-secondary">Annuler</a>
        <button class="btn btn-warning">{{ button_label|default('Enregistrer') }}</button>
    </div>
{{ form_end(form) }}
